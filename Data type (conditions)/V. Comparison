#include<stdio.h>
int main()
{
    int A,B;
    char x;
    scanf("%d %c %d", &A, &x, &B);
    if((x == '>' && A > B) || (x == '<' && A < B) || (x == '=' && A == B))
    {
        printf("Right");
    }
    else
    {
        printf("Wrong");
    }
    return 0;
}